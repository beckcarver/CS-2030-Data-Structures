#include "MYLIST.h"
#include <iostream>

using namespace std;

// functions for linked list

myList::insert (T item){
    if (head == nullptr){
        new node(item) tmp;
        head->tmp;
    } else {
        if (item > head.data)
    }
    size++;
};
myList::print();